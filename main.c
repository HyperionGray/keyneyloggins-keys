#include<unistd.h>
#include "src/log_keys.h"
#include "src/prepare.h"
#include "src/get_root.h"

int main() {

  const char *passfile_name = ""

  if (!check_root){
    get_root();
  }
//
  prepare();
  log_keys();

  return 0;
}
